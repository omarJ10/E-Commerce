{% extends 'temp.html.twig' %}

{% block title %}New Product{% endblock %}

{% block body %}
    <h1>Create new Product</h1>
    <div class="container">
    {{ include('product/_form.html.twig') }}

    <a class="btn btn-warning mt-4" href="{{ path('app_product_index') }}">back to list</a>
    </div>
{% endblock %}
