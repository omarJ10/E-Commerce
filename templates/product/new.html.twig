{% extends 'temp.html.twig' %}

{% block title %}New Product{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-md-12 mt-4">
            <div class="card">
                <div class="card-header">
                    <h3>
                        Ajouter Produit
                        <a class="btn btn-warning float-end"  href="{{ path('app_product_index') }}">back to list</a>
                    </h3>
                </div>
                <div class="card-body">
                    {{ include('product/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
</div>
</div>
{% endblock %}
